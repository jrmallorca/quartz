---
Created on: 2023-12-07
tags: [java, lombok]
deck: Zettelkasten
---

# Lombok `@EqualsAndHashCode` annotation

The [[lombok]] `@EqualsAndHashCode` annotation creates the following:

- `equals()` method,
- `hashCode()` method.

## Related content

- [Project Lombok](https://projectlombok.org/features/EqualsAndHashCode)

## Flashcards

In _Lombok_, what does the **`@EqualsAndHashCode` annotation** PROVIDE? :: `equals()` and `hashCode()` methods.^1704223241197
