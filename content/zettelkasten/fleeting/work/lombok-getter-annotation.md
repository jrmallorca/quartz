---
Created on: 2023-12-07
tags: [java, lombok]
deck: Zettelkasten
---

# Lombok `@Getter` annotation

The [[lombok]] `@Getter` annotation will generate the default getter for its respective annotated field.

For a field called `foo`, A default getter will be named `getFoo()` (or `isFoo()` for a boolean field) and returns `foo`.

## Related content

- [Project Lombok](https://projectlombok.org/features/GetterSetter)

## Flashcards

In _Lombok_, what does the **`@Getter` annotation** PROVIDE? :: A getter for the respectively annotated field.^1704223241144
